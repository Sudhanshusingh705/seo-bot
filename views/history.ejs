<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Visit History</title>
  <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Lora', serif;
      background-color: #000;
      color: #0f0;
      padding: 40px;
      margin: 0;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 30px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: #111;
      box-shadow: 0 0 10px #0f03;
    }

    th, td {
      padding: 12px 16px;
      text-align: left;
      border-bottom: 1px solid #333;
    }

    th {
      background: #222;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      color: #0f0;
      text-decoration: none;
    }

    .styled-button {
      display: inline-block;
      margin-top: 10px;
      padding: 10px 20px;
      font-size: 1.1rem;
      font-weight: bold;
      background: linear-gradient(135deg, #dc3545, #c82333);
      color: white;
      border: 2px solid #fff;
      border-radius: 5px;
      text-decoration: none;
    }

    .styled-button:hover {
      background: linear-gradient(135deg, #c82333, #b21d28);
    }
  </style>
</head>
<body>
  <h1>📜 Automation History</h1>
  <a href="/" class="styled-button">⬅️ Back to Dashboard</a>
  <a href="/history.csv" class="styled-button">📥 Export as CSV</a>

  <div class="history-section">
    <% if (Object.keys(history).length === 0) { %>
      <p>No visit history found.</p>
    <% } else { %>
      <% Object.keys(history).forEach(date => { %>
        <div class="date-group">
          <h3><%= date %></h3>
          <table>
            <thead>
              <tr>
                <th>URL</th>
                <th>Visited At</th>
                <th>Status</th>
              </tr>
            </thead>
            <tbody>
              <% history[date].forEach(entry => { %>
                <tr>
                  <td><%= entry.url %></td>
                  <td><%= new Date(entry.visitedAt).toLocaleString() %></td>
                  <td><%= entry.status %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>